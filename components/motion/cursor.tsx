/**
 * Cursor customizado — DESATIVADO (task #38 removeu do projeto).
 * Componente mantido como stub pra não quebrar imports legados.
 */
export function Cursor() {
  return null;
}
// end of Cursor
